# Core library for the Google Gemini API
google-generativeai==0.7.1

# For connecting to and interacting with Google Sheets
gspread==6.0.2

# Foundational libraries for Google Cloud authentication and API calls
google-api-python-client==2.136.0
google-auth-httplib2==0.2.0
google-auth-oauthlib==1.2.0

# For loading environment variables from a .env file (used in local development)
python-dotenv==1.0.1
